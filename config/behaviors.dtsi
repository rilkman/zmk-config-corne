/ {
    behaviors {
        mt {
            compatible = "zmk,behavior-hold-tap";
            label = "MT";
            flavor = "balanced";              // Options: "hold-preferred", "balanced", "tap-preferred", "tap-unless-interrupted"
            tapping-term-ms = <500>;          // Duration in milliseconds to distinguish tap from hold
        };
    };
};
